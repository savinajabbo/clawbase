# Public example does not require any keys.

# Coinbase Advanced Trade (authenticated)
# Create a key in Coinbase → API.
#   Key ID      → COINBASE_ADV_API_KEY
#   Private key → COINBASE_ADV_API_SECRET
COINBASE_ADV_API_KEY=
COINBASE_ADV_API_SECRET=

# Spotify Web API (OAuth PKCE)
# Create an app at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=
# Must match a Redirect URI configured in the Spotify dashboard.
# Recommended: http://127.0.0.1:8080/callback
SPOTIFY_REDIRECT_URI=
